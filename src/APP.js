const React = require('react');

function App() {
    
    return (
        <div>
            <h1>Hello World</h1>
            <button onClick={() => setCount(count + 1)}>+</button>
            <div></div>
        </div>
    );
}

module.exports = App
